import React from "react";
import ReactDOM from "react-dom";

import "normalize.css";
import "./styles/style.scss";

ReactDOM.render(
  <p> This is the expensify-app</p>,
  document.getElementById("app")
);
